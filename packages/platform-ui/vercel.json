{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] && exit 0; cd \"$(git rev-parse --show-toplevel)\" && git diff --quiet HEAD^ HEAD -- . ':!docs' ':!skills' ':!.claude' ':!agents' ':!.planning' ':!LICENSE' ':(exclude,glob)*.md'"
}

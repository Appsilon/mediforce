'use client';

// Re-export from the canonical hooks location for backwards compatibility.
// The implementation lives in src/hooks/use-namespace.ts.
export { useNamespace, type UseNamespaceResult } from '@/hooks/use-namespace';

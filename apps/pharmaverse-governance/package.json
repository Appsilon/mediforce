{
  "name": "@mediforce/pharmaverse-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build:scripts": "tsc -p tsconfig.json",
    "docker:build": "docker build -t mediforce-agent:pharmaverse-governance -f container/Dockerfile ../../"
  },
  "dependencies": {
    "@mediforce/platform-core": "workspace:*"
  }
}

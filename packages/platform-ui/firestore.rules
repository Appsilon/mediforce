rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator-only: allow all reads and writes for E2E testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

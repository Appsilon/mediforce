// Moved to `@mediforce/platform-core`, beside the `WorkflowSettings` type it
// has to agree with. Re-exported here so the editor's imports stay put.
export {
  pruneWorkflowSettings,
  type WorkflowSettingsDraft,
  type RegisterableWorkflowSettings,
} from '@mediforce/platform-core';

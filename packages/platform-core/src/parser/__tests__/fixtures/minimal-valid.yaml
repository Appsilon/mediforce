name: simple-process
version: "1.0"
steps:
  - id: start
    name: Start
    type: creation
  - id: done
    name: Done
    type: terminal
transitions:
  - from: start
    to: done
triggers:
  - type: manual
    name: Start Process

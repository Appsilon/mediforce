rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Task uploads: authenticated users can read/write under tasks/
    match /tasks/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.resource.size < 50 * 1024 * 1024  // 50MB max
                   && request.resource.contentType.matches('application/pdf|image/.*|text/csv');
    }
    match /agentSkills/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024;
    }
    match /namespaces/{handle}/logo/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 2 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
